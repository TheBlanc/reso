<nav class="navbar navbar-expand-md navbar-dark bg-dark px-0">
    <%= link_to "Reso", today_path, :class => "navbar-brand"  %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <%= link_to "This Week", today_path, :class => "nav-link"  %>
            </li>
            <li class="nav-item">
              <%= link_to "All Resos", reservations_path, :class => "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "New Reso", new_reservation_path, :class => "nav-link"  %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown">
                  <%= Restaurant.find(1).name %>
                </a>
                <div class="dropdown-menu">
                  <%= link_to "Profile", restaurant_path(1), :class => "dropdown-item text-center" %>
                  <%= link_to "Settings", edit_restaurant_path(1), :class => "dropdown-item text-center"  %>
                </div>
            </li>
        </ul>
    </div>
</nav>
